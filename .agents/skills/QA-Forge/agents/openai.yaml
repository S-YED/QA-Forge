interface:
  display_name: "QA Forge"
  short_description: "Repo-specific patterns and workflows for QA-Forge"
  default_prompt: "Use the QA-Forge repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true